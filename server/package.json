{
  "name": "lumos-server",
  "version": "1.0.0",
  "description": "- CRUD into database for Animation Name <-> User Group pair\r - Server is only to add to MQTT Broker and persist configs and metadata\r - Topic management for mqtt broker",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "mqtt": "^4.2.8"
  }
}
